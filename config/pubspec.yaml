name: psyconfig
publish_to: 'none'
version: 0.1.0

description: Centralized configuration authority for Psychosims.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  yaml: ^3.1.2
  meta: ^1.12.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
