name: psycore
publish_to: 'none'
version: 0.1.0

description: Pure deterministic core for Psychosims. No I/O, no model calls.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  psychemas:
    path: ../psychemas
  psyconfig:
    path: ../../config

dev_dependencies:
  crypto: ^3.0.3
  lints: ^3.0.0
  test: ^1.24.0
