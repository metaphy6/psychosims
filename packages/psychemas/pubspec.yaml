name: psychemas
publish_to: 'none'
version: 0.1.0

description: Shared canonical schemas for Psychosims (manifest, receipt, structured deltas).

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
