/// Typed route names for the Flutter client.
///
/// Deep-linking for desktop OAuth callbacks is stubbed; Phase 3 will populate
/// the parameter-bearing routes.
class AppRoutes {
  AppRoutes._();

  static const String home = '/';
  static const String session = '/session';
  static const String modelFetch = '/model-fetch';
}
