# Heavy native-model tests load/unload llama.cpp backends concurrently.
# Parallel execution causes intermittent timeouts on resource-constrained
# runners, so the app test suite runs sequentially.
concurrency: 1
