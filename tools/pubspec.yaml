name: psychosims_tools
publish_to: 'none'
version: 0.1.0

description: Headless tools for content and schema validation.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  psychemas:
    path: ../packages/psychemas
