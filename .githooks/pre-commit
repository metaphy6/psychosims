#!/usr/bin/env bash
set -euo pipefail

# Deterministic local gates; real model acceptance remains a separate gate.
# Installed by full bootstrap or scripts/install_git_hooks.sh.

cd "$(dirname "$0")/.."

make verify.contracts
