{
  "$schema": "https://modelcontextprotocol.io/schema/cursor-mcp.json",
  "$comment": "Cursor project-scoped MCP config. Workspace entries take precedence over global ones with the same name, so this is compatible with a global codegraph install. See docs/guides/MCP_SETUP.md.",
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@colbymchenry/codegraph", "serve", "--mcp", "--path", "${workspaceFolder}"]
    }
  }
}
