# 📦 Module: `<module-name>`

> Copy to `docs/code/MODULE-<name>.md`. Keep tight — one screen if possible.

## Purpose

One sentence: what problem this module solves.

## Public surface

| Symbol | Kind | Purpose |
|---|---|---|
| `DoTheThing()` | function | Entry point for X. |
| `Thing` | struct/class | Domain object representing Y. |

## Internals

How it works. One paragraph + diagram if useful.

## Invariants

- e.g. "`Thing.id` is immutable once `Save()` returns."

## Tests

- `path/to/module_test.go` — unit tests for happy path + edge cases.
- `path/to/module_integration_test.go` — touches the real DB.

## Dependencies

What this module depends on (other modules, external services).

## Known issues / future work

Brief. Link to ROADMAP entries where possible.
