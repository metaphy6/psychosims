# Showcase 2 — Therapy Deck & Loadout System (2.2)

_Slot cap + ownership · loadout-gap analysis · therapy controllers_

> Generated by `tools/showcase/` — a headless run of the Phase 2 deterministic core (no Flutter, no model).


## 1. Hard slot-cap and card-ownership enforcement

| loadout | cards | slot cap | within cap? | valid for library? |
| --- | --- | --- | --- | --- |
| full owned set | 4 | 6 | ✅ | ✅ |
| over cap | 4 | 2 | ❌ | ❌ |
| contains unowned card | 5 | 8 | ✅ | ❌ |

> The cap and ownership are enforced in the deterministic core, not only the UI — a receipt that references an un-equipped card is rejectable.


## 2. Loadout-gap analyzer — a mismatched loadout leaves a readable gap

| loadout | required signatures | present | missing | has gap? |
| --- | --- | --- | --- | --- |
| full owned set | breaker, buffer, gambit, freeze | breaker, buffer, gambit, freeze | — | ❌ |
| only `open_question` | breaker, buffer, gambit, freeze | breaker | buffer, gambit, freeze | ✅ |

- ✅ the full owned set covers the case with no gap
- ✅ a one-card loadout leaves a measurable, readable tactical gap


## 3. Focus & emotional-delivery controllers change resolution inputs

Resolving `open_question` from an identical state under different controller settings:

| controllers | Δ trust | Δ agitation | context fit |
| --- | --- | --- | --- |
| warm + childhood | 0 | 0 | mismatched |
| balanced | +1 | 0 | partial |
| objective + workspace | +2 | 0 | aligned |

> Controllers are typed core inputs — never free text — so the direct prompt-injection channel stays closed (0.12).

