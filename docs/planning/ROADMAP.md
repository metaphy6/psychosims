# 🗺 ROADMAP

> Single source of truth for sequenced work in this repository. Agents
> implement against this file phase-by-phase, drain every `[ ]` bullet in
> scope, append tracking rows, then stage.
>
> Generated by `xops/init/scaffold.sh` — replace the example phases below
> with your project's actual plan.

## 📊 Status snapshot

Update with `make roadmap.status` (parses the `[ ]` / `[x]` boxes below).

| Phase | Items | Done | Status |
|---|---|---|---|
| 1 — Foundation | 0 | 0 | 🟡 not started |
| 2 — Feature A   | 0 | 0 | ⚪ planned |
| 3 — Hardening   | 0 | 0 | ⚪ planned |

## 🧭 Guiding principles

- Smallest change that turns the test green.
- Tests move with code in the same commit.
- Reversible > impressive.
- One owner per phase; one tracking `run_id` per implementation pass.

## 📑 Table of contents

- [Phase 1 — Foundation](#phase-1--foundation)
- [Phase 2 — Feature A](#phase-2--feature-a)
- [Phase 3 — Hardening](#phase-3--hardening)
- [Appendix A — Tracking conventions](#appendix-a--tracking-conventions)
- [Appendix B — Definition of done](#appendix-b--definition-of-done)

---

## Phase 1 — Foundation

**Goal.** Stand the project up: repo skeleton, CI-equivalent local gates,
first smoke test.

**Scope id.** `phase-1`

**Test plan.** `make doctor` exits 0; the repo's primary test runner runs
zero tests but exits 0.

### Bullets

- [ ] Create top-level skeleton (`README.md`, license, gitignore).
- [ ] Stand up project's test runner with a single passing smoke test.
- [ ] Wire local lint/type-check gate.
- [ ] Document the `make` targets every contributor will use.

---

## Phase 2 — Feature A

**Goal.** _(replace this with the first real feature)_

**Scope id.** `phase-2`

**Test plan.** _(what tests must exist before this phase is "done")_

### Bullets

- [ ] _(replace with first user-visible deliverable for Feature A)_
- [ ] _(...)_

---

## Phase 3 — Hardening

**Goal.** Take the MVP from "works on my machine" to "safe to share".

**Scope id.** `phase-3`

**Test plan.** Threat model reviewed; error paths covered by tests; CI-equivalent
gate runs all tests + lint + type-check.

### Bullets

- [ ] Threat-model write-up in `docs/design/`.
- [ ] Error-path tests for every public API.
- [ ] Add a `CHANGELOG.md` and document the release process.

---

## Appendix A — Tracking conventions

- One `run_id` per implementation pass through a phase.
- `scope` column on every row = the phase id (e.g. `phase-2`).
- One `action=commit, status=completed, commit_sha=pending` row per logical
  commit, with the `summary` in Conventional Commits format. See
  [`docs/tracking/tracking.schema.md`](../tracking/tracking.schema.md).

## Appendix B — Definition of done

A phase is **done** when:

1. Every `[ ]` bullet under its heading is `[x]`.
2. The phase's *Test plan* line passes on a clean tree.
3. `make doctor` exits 0.
4. The status snapshot at the top of this file has been updated.
5. The phase's run produced one or more `commit` tracking rows whose
   `[run-id]` trailers all appear in `git log`.
